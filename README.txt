1) Run StudentMain.jar
2) Run TeacherMain.jar

Program will create a attendance.txt file in current working directory and store "x is present" in it where x is the name of the student which we declared
when creating new Student object (in the constructor argument).

After drawing any shape in teacher side, it might be needed to minimize and maximize (or drag outside the screen and bring it back) Student App frame in order to 
see drawed shapes in the student app. 
